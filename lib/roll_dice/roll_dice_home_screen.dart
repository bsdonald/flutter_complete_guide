import 'package:flutter/material.dart';

class RollDiceHomeScreen extends StatelessWidget {
  const RollDiceHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Roll Dice'),
      ),
      body: Container(),
    );
  }
}
